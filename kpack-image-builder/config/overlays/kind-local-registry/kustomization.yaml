apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - behavior: merge
    files:
      - controllersconfig/kpack_build_controllers_config.yaml
    name: config

resources:
  - ../../default
