---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cfspacequotas.korifi.cloudfoundry.org
spec:
  group: korifi.cloudfoundry.org
  names:
    kind: CFSpaceQuota
    listKind: CFSpaceQuotaList
    plural: cfspacequotas
    singular: cfspacequota
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.Name
      name: Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CFSpaceQuota is the Schema for the cfspacequota API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              apps:
                properties:
                  log_rate_limit_in_bytes_per_second:
                    format: int64
                    type: integer
                  per_app_tasks:
                    format: int64
                    type: integer
                  per_process_memory_in_mb:
                    format: int64
                    type: integer
                  total_instances:
                    format: int64
                    type: integer
                  total_memory_in_mb:
                    format: int64
                    type: integer
                type: object
              name:
                type: string
              routes:
                properties:
                  total_reserved_ports:
                    format: int64
                    type: integer
                  total_routes:
                    format: int64
                    type: integer
                type: object
              services:
                properties:
                  paid_services_allowed:
                    type: boolean
                  total_service_instances:
                    format: int64
                    type: integer
                  total_service_keys:
                    format: int64
                    type: integer
                type: object
            required:
            - name
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
