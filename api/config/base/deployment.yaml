apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: korifi-api
  name: deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: korifi-api
  strategy: {}
  template:
    metadata:
      labels:
        app: korifi-api
    spec:
      serviceAccountName: system-serviceaccount
      containers:
      - image: cloudfoundry/korifi-api:latest
        name: korifi-api
        ports:
        - containerPort: 9000
          name: web
        resources:
          requests:
            cpu: 50m
            memory: 100Mi
        env:
        - name: APICONFIG
          value: "/etc/korifi-api-role-mappings"
        - name: TLSCONFIG
          value: "/etc/korifi-tls-config"
        - name: EXTERNAL_FQDN
          value: "api.example.org"
        - name: INTERNAL_PORT
          value: "9000"
        - name: ROOT_NAMESPACE
          value: cf
        - name: LIFECYCLE_TYPE
          value: buildpack
        - name: LIFECYCLE_STACK
          value: cflinuxfs3
        - name: LIFECYCLE_STAGING_MEMORY_MB
          value: "1024"
        - name: LIFECYCLE_STAGING_DISK_MB
          value: "1024"
        - name: PACKAGE_REGISTRY_BASE
          value: "gcr.io/cf-relint-greengrass/korifi-controllers/kpack/beta"
        - name: PACKAGE_REGISTRY_SECRET
          value: image-registry-credentials
        - name: DEFAULT_DOMAIN_NAME
          value: apps.example.org
        volumeMounts:
        - name: &configname korifi-api-role-mappings
          mountPath: /etc/korifi-api-role-mappings
          readOnly: true
        - name: &tlsname korifi-tls-config
          mountPath: /etc/korifi-tls-config
          readOnly: true
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
      volumes:
      - name: *configname
        configMap:
          name: korifi-api-role-mappings
      - name: *tlsname
        secret:
          secretName: korifi-api-internal-cert
